package hw;

public class HomeworkFour {
    public static void main(String[] args) {
        int vika = 1;
        int max = 1;
        int ruslan = 31;
        int alina = 28;
        int age = vika + max + ruslan + alina;
//        double age = vika + max + ruslan + alina; альтернативный способ через double
//        double average = age / 4;
//        double average = (double)age / 4.0; еще один способ через дробное число
        double average = (double)age / 4;
        System.out.println(average);
    }
}
